<!-- Filename: module/Blog/view/blog/list/index.phtml -->
<h1>Blog\ListController::indexAction()</h1>
<?php


foreach ($this->posts as $post) {
	var_dump($post->getId(), $post->getTitle(), $post->getText());
}

foreach ($this->sqls as $sql) {
	var_dump($sql);
}